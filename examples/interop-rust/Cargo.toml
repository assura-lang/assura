# Standalone sample (not a workspace member).
[workspace]

[package]
name = "assura-interop-demo"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"
description = "Sample: normal Rust lib + assura check-rust annotations"

[lib]
path = "src/lib.rs"

// EXPECTED: verified
// Arithmetic contract that should VERIFY
// If x > 5, then x + 1 > 5 is trivially true

contract IncrementPositive {
    requires { x > 5 }
    requires { x + 1 > x }
    ensures  { x + 1 > 5 }
}

// If a >= 0 and b >= 0, then a + b >= 0

contract SumNonNegative {
    requires { a >= 0 }
    requires { b >= 0 }
    requires { a + b >= a }
    ensures  { a + b >= 0 }
}

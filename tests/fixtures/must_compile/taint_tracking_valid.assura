// MUST COMPILE
// Function processing tainted input with validation.
module test.taint_valid;

fn process_input(data: tainted String) -> String
    requires { data.length() > 0 }
    ensures { result.length() > 0 }

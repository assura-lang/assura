// MUST COMPILE
// Valid constant-time function with secret input.
module test.ct_valid;

fn constant_add(x: secret Int, y: Int) -> Int
    constant_time true
    requires { y >= 0 }
    ensures { result >= y }

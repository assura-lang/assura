// LLM Benchmark 05: Clamp value to range
// Difficulty: easy
// Expected: result in [lo, hi]
contract Clamp {
    input(x: Int, lo: Int, hi: Int)
    output(result: Int)
    requires { lo <= hi }
    ensures { result >= lo }
    ensures { result <= hi }
}

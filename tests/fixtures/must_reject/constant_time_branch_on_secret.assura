// MUST REJECT A14001
// BLOCKED: #145 -- checker needs secret-annotated params and branch-on-secret detection in clause bodies
module test.ct_violation;

fn secret_compare(s: secret Int, threshold: Int) -> Bool
    constant_time true
    requires { s > 0 }
    ensures { result == true }

// MUST REJECT A14001
module test.ct_violation;

fn secret_compare(s: secret Int, threshold: Int) -> Bool
    constant_time true
    requires { s > 0 }
    ensures { if s > threshold then true else false }


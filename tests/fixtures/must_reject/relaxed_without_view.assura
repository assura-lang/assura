// MUST REJECT A-CONC-016
// Relaxed memory ordering with ensures clause: the read value may be
// stale, so value-dependent assertions are unsound without a view check.
contract StaleRelaxedRead {
    input(shared_counter: Int)
    ordering: relaxed
    ensures { shared_counter > 0 }
}

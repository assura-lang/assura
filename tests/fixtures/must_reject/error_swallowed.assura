// MUST REJECT A12002
// BLOCKED: static A12002 from must_not_mask + catch/translate not yet produced by run_error_propagation_checks (SMT sees the policy as Unknown)
module test.error_swallowed;

contract FilePolicy {
    input(path: String)
    must_not_mask { IoError GenericError }
    requires { true }
    ensures { true }
}

fn read_file(path: String) -> Result
    catch IoError translate_to GenericError
    requires { true }
    ensures { true }


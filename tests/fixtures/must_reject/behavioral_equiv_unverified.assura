// MUST REJECT A49001
// BLOCKED: #122 -- clause body tokens not parsed as BinOp expression
module test.equiv_unverified;

contract SortEquiv {
    input(n: Nat)
    equivalent sort_v1 == sort_v2
    requires { n > 0 }
    ensures { n > 0 }
}

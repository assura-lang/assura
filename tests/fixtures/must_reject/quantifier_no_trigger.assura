// MUST REJECT A53006
module test.quantifier_trigger;

contract SortedArray {
    input(arr: List<Int>, x: Int)
    strict_triggers true
    requires { forall i in arr: i >= 0 }
    ensures { result >= 0 }
}

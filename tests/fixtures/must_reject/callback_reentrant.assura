// MUST REJECT A24001
module test.reentrant;

contract Vault {
    input(amount: Nat)
    non_reentrant withdraw
    requires { withdraw > 0 }
    ensures { amount > 0 }
}

fn withdraw(amount: Nat) -> Nat
    requires { amount > 0 }
    ensures { amount > 0 }


# Catalog codes with no production emit site (hollow / SPEC placeholders).
# guards.sh section 15 freezes this set: new hollow codes FAIL CI.
# Pure-listing codes (no partial implementation) were removed from catalog + SPEC.
# When you wire a code, remove it from this file in the same PR.
# Sentinels A00000/A88888/A99999 are never listed.
# Ref: issue #1490.

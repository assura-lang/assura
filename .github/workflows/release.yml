# cargo-dist installers + crates.io publish (tag or workflow_dispatch).
#
# Release PR lifecycle lives in release-please.yml. When that workflow creates a
# release, it dispatches this workflow with the required tag input. Human tag
# push still works as break-glass.

name: Release
permissions:
  contents: read

on:
  push:
    tags:
      - '**[0-9]+.[0-9]+.[0-9]+*'
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v0.1.0) for cargo-dist + crates.io publish"
        required: true
        type: string

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Run 'dist plan' (or host) to determine what tasks we need to do
  plan:
    # Tag push (break-glass) or workflow_dispatch with required tag input
    # (from release-please.yml on release_created, or manual re-run).
    if: >-
      startsWith(github.ref, 'refs/tags/') ||
      (github.event_name == 'workflow_dispatch' && inputs.tag != '')
    runs-on: "ubuntu-24.04"
    timeout-minutes: 10
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ steps.resolve-tag.outputs.tag }}
      tag-flag: ${{ steps.resolve-tag.outputs.tag && format('--tag={0}', steps.resolve-tag.outputs.tag) || '' }}
      publishing: ${{ steps.resolve-tag.outputs.tag != '' }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - name: Resolve release tag
        id: resolve-tag
        env:
          DISPATCH_TAG: ${{ inputs.tag }}
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          elif [[ -n "${DISPATCH_TAG}" ]]; then
            echo "tag=${DISPATCH_TAG}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=" >> "$GITHUB_OUTPUT"
          fi
      # Tag push: build the tagged commit.
      # workflow_dispatch: build the selected branch tip (github.sha) so a
      # post-tag fix on main can re-publish the same announcement tag without
      # force-moving the git tag (v0.1.0 first cut failed before dist=true).
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          submodules: recursive
          ref: ${{ github.event_name == 'workflow_dispatch' && github.sha || steps.resolve-tag.outputs.tag || github.ref }}
      - name: Install dist
        # we specify bash to get pipefail; it guards against the `curl` command
        # failing. otherwise `sh` won't catch that `curl` returned non-0
        shell: bash
        run: "curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v0.32.0/cargo-dist-installer.sh | sh"
      - name: Cache dist
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/dist
      # sure would be cool if github gave us proper conditionals...
      # so here's a doubly-nested ternary-via-truthiness to try to provide the best possible
      # functionality based on whether this is a pull_request, and whether it's from a fork.
      # (PRs run on the *source* but secrets are usually on the *target* -- that's *good*
      # but also really annoying to build CI around when it needs secrets to work right.)
      # Always use the resolved release tag (tag push or workflow_dispatch input).
      # Do not use github.ref_name here: on workflow_dispatch from main it is
      # "main", which cargo-dist cannot parse as a version.
      - id: plan
        env:
          RELEASE_TAG: ${{ steps.resolve-tag.outputs.tag }}
        run: |
          if [[ -z "${RELEASE_TAG}" ]]; then
            echo "No release tag resolved; refusing to plan a publish." >&2
            exit 1
          fi
          dist host --steps=create --tag="${RELEASE_TAG}" --output-format=json > plan-dist-manifest.json
          echo "dist ran successfully"
          cat plan-dist-manifest.json
          echo "manifest=$(jq -c "." plan-dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-plan-dist-manifest
          path: plan-dist-manifest.json

  # Build and packages all the platform-specific things
  build-local-artifacts:
    name: build-local-artifacts (${{ join(matrix.targets, ', ') }})
    # Let the initial task tell us to not run (currently very blunt)
    needs:
      - plan
    # always() prevents transitive skip when release-please is skipped (tag/dispatch).
    if: ${{ always() && !failure() && !cancelled() && needs.plan.result == 'success' && fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix.include != null && (needs.plan.outputs.publishing == 'true' || fromJson(needs.plan.outputs.val).ci.github.pr_run_mode == 'upload') }}
    strategy:
      fail-fast: false
      # Target platforms/runners are computed by dist in create-release.
      # Each member of the matrix has the following arguments:
      #
      # - runner: the github runner
      # - dist-args: cli flags to pass to dist
      # - install-dist: expression to run to install dist on the runner
      #
      # Typically there will be:
      # - 1 "global" task that builds universal installers
      # - N "local" tasks that build each platform's binaries and platform-specific installers
      matrix: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    container: ${{ matrix.container && matrix.container.image || null }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # z3-sys gh-release uses the GitHub API; unauthenticated calls hit rate limits
      # (assert status==200 fails on aarch64). Authenticated token avoids that.
      READ_ONLY_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/${{ join(matrix.targets, '-') }}-dist-manifest.json
    steps:
      - name: Harden runner
        if: runner.os == 'Linux'
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - name: enable windows longpaths
        run: |
          git config --global core.longpaths true
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install Rust non-interactively if not already installed
        if: ${{ matrix.container }}
        run: |
          if ! command -v cargo > /dev/null 2>&1; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          fi
      - name: Install dist
        run: ${{ matrix.install_dist.run }}
      # Get the dist-manifest
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - name: Install dependencies
        run: |
          ${{ matrix.packages_install }}
      - name: Build artifacts
        run: |
          # Actually do builds and make zips and whatnot
          dist build ${{ needs.plan.outputs.tag-flag }} --print=linkage --output-format=json ${{ matrix.dist_args }} > dist-manifest.json
          echo "dist ran successfully"
      - id: cargo-dist
        name: Post-build
        # We force bash here just because github makes it really hard to get values up
        # to "real" actions without writing to env-vars, and writing to env-vars has
        # inconsistent syntax between shell and powershell.
        shell: bash
        run: |
          # Parse out what we just built and upload it to scratch storage
          {
            echo "paths<<EOF"
            dist print-upload-files-from-manifest --manifest dist-manifest.json
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-build-local-${{ join(matrix.targets, '_') }}
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}

  # Build and package all the platform-agnostic(ish) things
  build-global-artifacts:
    needs:
      - plan
      - build-local-artifacts
    # always() prevents transitive skip when release-please is skipped (tag/dispatch).
    if: ${{ always() && !failure() && !cancelled() && needs.plan.result == 'success' && needs.plan.outputs.publishing == 'true' }}
    runs-on: "ubuntu-24.04"
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      READ_ONLY_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/global-dist-manifest.json
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install cached dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      # Get all the local artifacts for the global tasks to use (for e.g. checksums)
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: cargo-dist
        shell: bash
        run: |
          dist build ${{ needs.plan.outputs.tag-flag }} --output-format=json "--artifacts=global" > dist-manifest.json
          echo "dist ran successfully"

          # Parse out what we just built and upload it to scratch storage
          {
            echo "paths<<EOF"
            jq --raw-output ".upload_files[]" dist-manifest.json
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-build-global
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}
  # Determines if we should publish/announce
  host:
    needs:
      - plan
      - build-local-artifacts
      - build-global-artifacts
    # Only run if we're "publishing", and only if plan, local and global didn't fail (skipped is fine)
    # always() + !cancelled: release-please may be skipped on tag/dispatch paths.
    if: ${{ always() && !cancelled() && needs.plan.result == 'success' && needs.plan.outputs.publishing == 'true' && (needs.build-global-artifacts.result == 'skipped' || needs.build-global-artifacts.result == 'success') && (needs.build-local-artifacts.result == 'skipped' || needs.build-local-artifacts.result == 'success') }}
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    runs-on: "ubuntu-24.04"
    timeout-minutes: 15
    outputs:
      val: ${{ steps.host.outputs.manifest }}
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      # workflow_dispatch: build notes/scripts from branch tip; tag push: use the tag.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          submodules: recursive
          ref: ${{ github.event_name == 'workflow_dispatch' && github.sha || needs.plan.outputs.tag }}
      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable
      # Create/reuse GitHub Release and attach cargo-dist artifacts.
      # Do NOT run `dist host --steps=release`: release-please already creates
      # the GitHub Release when its PR merges, and a second create fails.
      - name: "Download GitHub Artifacts"
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: artifacts
          merge-multiple: true
      - name: Create or update GitHub Release
        id: host
        env:
          PRERELEASE_FLAG: "${{ fromJson(needs.plan.outputs.val).announcement_is_prerelease && '--prerelease' || '' }}"
          ANNOUNCEMENT_TITLE: "${{ fromJson(needs.plan.outputs.val).announcement_title }}"
          RELEASE_COMMIT: "${{ github.sha }}"
          TAG: ${{ needs.plan.outputs.tag }}
        shell: bash
        run: |
          set -euo pipefail
          # Prefer cargo-dist plan notes when present; release-please body stays
          # if we only upload to an existing release.
          plan_manifest=""
          if [ -f artifacts/plan-dist-manifest.json ]; then
            plan_manifest=artifacts/plan-dist-manifest.json
          else
            # download-artifact may nest by artifact name
            plan_manifest=$(find artifacts -name 'plan-dist-manifest.json' 2>/dev/null | head -1 || true)
          fi
          if [ -n "${plan_manifest}" ] && [ -f "${plan_manifest}" ]; then
            jq -r '.announcement_github_body // empty' "${plan_manifest}" > "$RUNNER_TEMP/notes.txt"
            jq -c '{ci, announcement_is_prerelease, announcement_title, releases, publish_prereleases}' \
              "${plan_manifest}" > "$RUNNER_TEMP/slim-manifest.json"
          else
            : > "$RUNNER_TEMP/notes.txt"
            echo '{}' > "$RUNNER_TEMP/slim-manifest.json"
          fi

          # Drop granular manifests so only distributable files are uploaded.
          find artifacts -name '*-dist-manifest.json' -delete 2>/dev/null || true

          mapfile -t asset_files < <(find artifacts -type f ! -name '.*' | sort)
          if [[ ${#asset_files[@]} -eq 0 ]]; then
            echo "error: no release artifacts found under artifacts/" >&2
            exit 1
          fi

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists (created by release-please). Uploading artifacts..."
            gh release upload "$TAG" "${asset_files[@]}" --clobber
          else
            # shellcheck disable=SC2086 # PRERELEASE_FLAG is empty or --prerelease
            gh release create "$TAG" --target "$RELEASE_COMMIT" $PRERELEASE_FLAG \
              --title "${ANNOUNCEMENT_TITLE:-$TAG}" \
              --notes-file "$RUNNER_TEMP/notes.txt" \
              "${asset_files[@]}"
          fi

          # Optional curated override (commit RELEASE_NOTES.md to main before merge).
          if [ -f RELEASE_NOTES.md ]; then
            echo "Applying RELEASE_NOTES.md override..."
            gh release edit "$TAG" --notes-file RELEASE_NOTES.md
          fi

          echo "manifest=$(cat "$RUNNER_TEMP/slim-manifest.json")" >> "$GITHUB_OUTPUT"

      - name: Generate SBOM
        run: |
          set -euo pipefail
          cargo install cargo-cyclonedx --locked
          # Workspace mode writes one JSON per package under crates/*/ (not repo root).
          # SBOM the published CLI package; that is what the release ships as a binary.
          cargo cyclonedx --format json \
            --manifest-path crates/assura-cli/Cargo.toml \
            --override-filename assura-sbom
          if [ -f crates/assura-cli/assura-sbom.json ]; then
            cp crates/assura-cli/assura-sbom.json assura-sbom.cdx.json
          elif [ -f crates/assura-cli/assura-sbom.cdx.json ]; then
            cp crates/assura-cli/assura-sbom.cdx.json assura-sbom.cdx.json
          else
            echo "error: no CycloneDX SBOM produced for assura-cli" >&2
            find crates/assura-cli -maxdepth 2 -type f | head -50 >&2 || true
            exit 1
          fi
          test -s assura-sbom.cdx.json
          echo "SBOM bytes: $(wc -c < assura-sbom.cdx.json)"

      - name: Upload SBOM to release
        run: gh release upload "${{ needs.plan.outputs.tag }}" assura-sbom.cdx.json --clobber

      - name: Attest build provenance
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-path: artifacts/*

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign release artifacts
        run: |
          for artifact in artifacts/*; do
            [ -f "$artifact" ] || continue
            echo "Signing ${artifact}..."
            cosign sign-blob --yes --bundle "${artifact}.sigstore.json" "${artifact}"
          done
          echo "Signing SBOM..."
          cosign sign-blob --yes --bundle assura-sbom.cdx.json.sigstore.json assura-sbom.cdx.json

      - name: Upload signatures to release
        run: |
          gh release upload "${{ needs.plan.outputs.tag }}" \
            artifacts/*.sigstore.json \
            assura-sbom.cdx.json.sigstore.json \
            --clobber

  # Publish all workspace crates to crates.io in dependency order
  publish-crates:
    needs:
      - plan
      - host
    if: ${{ always() && !cancelled() && needs.host.result == 'success' && needs.plan.outputs.publishing == 'true' }}
    runs-on: "ubuntu-24.04"
    # Multi-crate first cuts: 60s between new uploads + 429 backoff (see #811).
    timeout-minutes: 90
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          ref: ${{ github.event_name == 'workflow_dispatch' && github.sha || needs.plan.outputs.tag }}

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30 # stable

      - name: Install system dependencies
        run: sudo apt-get install -y protobuf-compiler libz3-dev
        env:
          DEBIAN_FRONTEND: noninteractive

      - name: Set Linux Z3 paths
        run: |
          {
            echo "Z3_SYS_Z3_HEADER=/usr/include/z3.h"
            echo "CPATH=/usr/include"
            echo "LIBRARY_PATH=/usr/lib/x86_64-linux-gnu"
          } >> "$GITHUB_ENV"

      - name: Publish crates in dependency order
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: bash scripts/publish-crates.sh

  announce:
    needs:
      - plan
      - host
      - publish-crates
    # Wait for publish while still allowing publish to be the only blocker when host ok.
    if: ${{ always() && !cancelled() && needs.host.result == 'success' && (needs.publish-crates.result == 'skipped' || needs.publish-crates.result == 'success') }}
    runs-on: "ubuntu-24.04"
    timeout-minutes: 5
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      TAG: ${{ needs.plan.outputs.tag }}
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - name: Announce release complete
        env:
          # gh release view may invoke git; no checkout on this job.
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          echo "Release pipeline finished for tag: ${TAG}"
          gh api "repos/${GH_REPO}/releases/tags/${TAG}" \
            --jq '"url=\(.html_url) tag=\(.tag_name) draft=\(.draft) prerelease=\(.prerelease)"'

  # Remove curated RELEASE_NOTES.md from main after it was applied to the
  # GitHub Release body (host job). Branch protection requires a PR; use the
  # auto-approve app so the cleanup PR can auto-merge. Non-fatal: notes left
  # on main are wrong only for the *next* release override window.
  cleanup-release-notes:
    needs:
      - plan
      - host
      - announce
    if: ${{ always() && !cancelled() && needs.host.result == 'success' }}
    runs-on: "ubuntu-24.04"
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Harden runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit
      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: cleanup-token
        continue-on-error: true
        with:
          client-id: ${{ vars.AUTO_APPROVE_CLIENT_ID }}
          private-key: ${{ secrets.AUTO_APPROVE_PRIVATE_KEY }}
      - name: Open PR to remove RELEASE_NOTES.md
        continue-on-error: true
        env:
          GH_TOKEN: ${{ steps.cleanup-token.outputs.token || secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.plan.outputs.tag }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          if ! gh api "repos/${GH_REPO}/contents/RELEASE_NOTES.md?ref=main" --jq '.sha' >/dev/null 2>&1; then
            echo "No RELEASE_NOTES.md on main; nothing to clean up."
            exit 0
          fi
          BRANCH="chore/cleanup-release-notes-${TAG}"
          # Reuse branch if a prior cleanup attempt left it.
          if ! gh api "repos/${GH_REPO}/git/ref/heads/${BRANCH}" >/dev/null 2>&1; then
            MAIN_SHA=$(gh api "repos/${GH_REPO}/git/ref/heads/main" --jq '.object.sha')
            gh api "repos/${GH_REPO}/git/refs" \
              -f ref="refs/heads/${BRANCH}" \
              -f sha="${MAIN_SHA}"
          fi
          if gh api "repos/${GH_REPO}/contents/RELEASE_NOTES.md?ref=${BRANCH}" --jq '.sha' >/dev/null 2>&1; then
            FILE_SHA=$(gh api "repos/${GH_REPO}/contents/RELEASE_NOTES.md?ref=${BRANCH}" --jq '.sha')
            gh api --method DELETE "repos/${GH_REPO}/contents/RELEASE_NOTES.md" \
              -f message="chore: remove release notes override after ${TAG}" \
              -f sha="${FILE_SHA}" \
              -f branch="${BRANCH}"
          else
            echo "RELEASE_NOTES.md already absent on ${BRANCH}"
          fi
          EXISTING=$(gh pr list --repo "${GH_REPO}" --head "${BRANCH}" --state open \
            --json number --jq '.[0].number // empty')
          if [ -n "${EXISTING}" ]; then
            echo "Cleanup PR already open: #${EXISTING}"
            gh pr merge "${EXISTING}" --repo "${GH_REPO}" --auto --squash || true
            exit 0
          fi
          # If the branch tip already matches main (file gone via earlier merge), skip.
          if ! gh api "repos/${GH_REPO}/contents/RELEASE_NOTES.md?ref=main" >/dev/null 2>&1 \
            && ! gh api "repos/${GH_REPO}/contents/RELEASE_NOTES.md?ref=${BRANCH}" >/dev/null 2>&1; then
            echo "RELEASE_NOTES.md already gone from main and ${BRANCH}"
            exit 0
          fi
          PR_URL=$(gh pr create --repo "${GH_REPO}" --base main --head "${BRANCH}" \
            --title "chore: remove release notes override" \
            --body "Auto-cleanup after ${TAG}: curated RELEASE_NOTES.md was applied to the GitHub Release and should not stay on main for the next cut.")
          gh pr merge "$PR_URL" --repo "${GH_REPO}" --auto --squash || true
          echo "Opened cleanup PR: ${PR_URL}"
